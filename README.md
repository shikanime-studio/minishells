# minishells

This repo is where I park the Nix/devenv shells I actually use for the
different open source organisations I work with. Instead of reinventing a
dev environment every time, I plug in devlib once and spin up a shell per
project.
